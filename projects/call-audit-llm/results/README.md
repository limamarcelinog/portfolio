# results

Empty on purpose.

`score.py --out` and `reliability.py --out` write here. I have not run the
pipeline against the live API, so there is no sample run to commit — and a
fabricated one would defeat the point of a project whose subject is whether a
generated number can be trusted.
